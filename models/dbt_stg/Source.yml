
version: 2
sources:
  - name: datafeed_shared_schema
    database: "{{('sales')}}"
    schema: dbt_source

    tables:
      - name: CUSTOMERS

  - name: datafeed_shared_schema_dev
    database: "{{('sales')}}"
    schema: dev

    tables:
      - name: RAW_LINEITEM
      - name: RAW_ORDER
      - name: RAW_CUSTOMER
      - name: RAW_NATION
